var scheme = schema({
		name: String,
		email: String,
		password: String,
		description: String,
		rating: Number,
		notifications: Number,
		freelancer: String,
		payout_email: String,
		admin: Boolean,
		banned: Boolean
});